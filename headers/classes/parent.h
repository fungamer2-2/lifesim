#ifndef PARENT_H
#define PARENT_H

#include "./person.h"

namespace lsim {
    class Parent : public lsim::Person {
        public:
            Parent(bool nSex);
        private:
    }; // class Parent
} // namespace lsim

#endif // ifndef PARENT_H